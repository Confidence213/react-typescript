import React from "react";

import SearchForm from "./SearchForm";

const App: React.FC = () => {
  return (
    <div id="app" className="container">
      <SearchForm />
    </div>
  );
};

export default App;
